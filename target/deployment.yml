apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-toolchain-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-toolchain-demo
  template:
    metadata:
      labels:
        app: kube-toolchain-demo
    spec:
      containers:
      - name:
        image:
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
